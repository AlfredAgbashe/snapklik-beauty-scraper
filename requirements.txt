# R Package Dependencies for Snapklik Beauty Products Web Scraper
# Install with: install.packages(c("chromote", "rvest", "dplyr", "stringr"))

# Core web scraping packages
chromote >= 0.1.1    # Headless Chrome automation for JavaScript-heavy sites
rvest >= 1.0.3       # HTML parsing and web scraping

# Data manipulation and processing
dplyr >= 1.1.0       # Data manipulation and transformation
stringr >= 1.5.0     # String manipulation and regex operations

# Optional packages for enhanced functionality
jsonlite >= 1.8.0    # JSON parsing (if needed for API calls)
httr >= 1.4.4        # HTTP requests (for additional data sources)
readr >= 2.1.3       # Fast CSV reading/writing
tibble >= 3.1.8      # Modern data frames

# Development and debugging (optional)
# devtools >= 2.4.5  # Development tools
# testthat >= 3.1.5  # Unit testing framework

# Installation instructions:
# 1. Open R or RStudio
# 2. Run: install.packages(c("chromote", "rvest", "dplyr", "stringr"))
# 3. For optional packages: install.packages(c("jsonlite", "httr", "readr", "tibble"))

# Note: chromote requires Chrome or Chromium browser to be installed on the system
# Chrome download: https://www.google.com/chrome/
